[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nazarai-seg"
version = "0.1.0"
description = "Quick semantic segmentation training study."
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
